# ShopIntel Deployment Guide

## Prerequisites

- Docker & Docker Compose
- PostgreSQL (if not using Docker)
- Redis (if not using Docker)
- Python 3.9+
- Node.js 18+

## Quick Start with Docker

1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd shopintel